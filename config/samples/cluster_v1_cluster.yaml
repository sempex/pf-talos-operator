apiVersion: cluster.sempex/v1
kind: Cluster
metadata:
  labels:
    app.kubernetes.io/name: pf-talos-operator
    app.kubernetes.io/managed-by: kustomize
  name: cluster-sample
spec:
  # TODO(user): Add fields here
